import { Routes } from '@angular/router';
import { NotificationLookupComponent } from './notification-lookup/notification-lookup.component';

export const routes: Routes = [
  { path: '', component: NotificationLookupComponent },
];
