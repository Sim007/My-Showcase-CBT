{
  "name": "contract-testing-showcase",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:type0": "npx playwright test --project 'type0 – binnen deelsysteem (Angular UI → Backend)'",
    "test:type1": "npx playwright test --project 'type1 – tussen deelsystemen (Order → Payment)'",
    "test:type2": "npx playwright test --project 'type2 – queue (Payment → Notification)'",
    "test:type3": "npx playwright test --project 'type3 – extern SOAP (Payment → WireMock)'"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "typescript": "~5.4.0"
  },
  "overrides": {
    "rimraf": "^5.0.0",
    "glob": "^11.0.0",
    "tar": "^7.0.0"
  }
}
